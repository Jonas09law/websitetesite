<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo %></title>
    <%- include('./partials/protection') %>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <nav class="navbar">
        <div class="nav-left">
            <div class="logo">
                <img src="/images/logo.svg" alt="Logo">
                TaxadoS
            </div>
        </div>
        <div class="nav-center">
            <a href="/" class="nav-link">Início</a>
            <a href="/scammers" class="nav-link">Scammers</a>
            <a href="/termos" class="nav-link">Termos</a>
            <a href="/parceiros" class="nav-link">Parceiros</a>
        </div>
        <div class="nav-right">
            <a href="/discord" class="nav-link">Discord</a>
            <% if (user) { %>
                <div class="user-profile" id="userProfile">
                    <img src="<%= user.avatar ? `https://cdn.discordapp.com/avatars/${user.id}/${user.avatar}.png` : '/images/default-avatar.png' %>" alt="Avatar" class="avatar">
                    <div class="dropdown-menu">
                        <div class="dropdown-header">
                            <div class="user-info">
                                <img src="<%= user.avatar ? `https://cdn.discordapp.com/avatars/${user.id}/${user.avatar}.png` : '/images/default-avatar.png' %>" alt="Avatar" class="avatar">
                                <div class="user-details">
                                    <span class="user-name"><%= user.username %></span>
                                    <span class="user-email"><%= user.email %></span>
                                </div>
                            </div>
                        </div>
                        <div class="dropdown-items">
                            <a href="/dashboard" class="dropdown-item">
                                <img src="/images/dashboard-icon.svg" alt="Dashboard">
                                Dashboard
                            </a>
                            <a href="/logout" class="dropdown-item danger">
                                <img src="/images/logout-icon.svg" alt="Logout">
                                Sair
                            </a>
                        </div>
                    </div>
                </div>
            <% } else { %>
                <a href="/login" class="login-button">Login</a>
            <% } %>
        </div>
    </nav>

    <main>
        <div class="hero-section">
            <h1>TaxadoS Scammers</h1>
            <p class="subtitle">Não caia em golpes, verifique as denúncias da comunidade.</p>
            <a href="/scammers" class="cta-button">Ver Scammers <span class="arrow">→</span></a>
        </div>
    </main>

    <script src="/js/main.js"></script>
</body>
</html> 